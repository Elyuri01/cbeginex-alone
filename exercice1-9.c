# include <stdio.h>

// copy input to output and replace each string of blanks with a single blank

int main () 
{
    int c;
   
    while ((c = getchar()) != EOF)
	    putchar(c);
    
    return 0; 
}
